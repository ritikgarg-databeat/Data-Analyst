# Pinned, reproducible environment for the Python Lab sandbox image.
# Verified mutually compatible together under Python 3.12 — see
# apps/api/tests/test_python_kernel.py, which installs this same set (via
# apps/api's `sandbox` optional-dependency group) to exercise kernel.py
# directly. Bump both together.

pandas==3.0.5
numpy==2.5.2
polars==1.44.1
pyarrow==25.0.1
scipy==1.18.1
statsmodels==0.15.0
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
plotly==7.0.0
openpyxl==3.1.5
requests==2.34.2
python-dateutil==2.9.0.post0
